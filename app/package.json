{
  "name": "app",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun run build.ts",
    "watch": "bun run build.ts --watch",
    "dev": "NODE_ENV=development bun run build.ts --watch",
    "build:prod": "NODE_ENV=production bun run build.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "@types/chrome": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@types/node": "^24.8.1",
    "pocketbase": "^0.26.2"
  }
}
